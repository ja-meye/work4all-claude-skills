# Versionshistorie — `fix-folgeseiten-uebertrag-problem` (DXJ0001)

Ausgelagert aus `SKILL.md` (Progressive Disclosure: die Historie wird nur gelesen, wenn sie gebraucht wird —
sie muss nicht bei jedem Trigger mitgeladen werden).

## Versionen

- v1.12.0 — Muster (i) Punkt 4 in `fix-catalog.md` um eine verbindliche Ergänzung geschärft (Report `dxAio_template`, M001, 10.09.2026, Nutzer-Rückmeldung nach eigenem Testdruck): Ein Fix-Lauf hatte beim Band-Split-Fix (Muster i) nur das im selben Schritt neu angelegte Helper-Control (`lblDocGesamtGesPreis`) auf die erzwungene Minimalhöhe abgesenkt — zwei bereits vorher im selben Band (`Sub_POS_Ueberschriften_Folgeseiten`) vorhandene Geschwister-Controls (`xrLabel45`, `lblCarryHelperOben`, beide über `<LocalizationItems>` persistiert) blieben unangetastet und erzeugten beim Testdruck zusätzlichen Abstand. Punkt 4 verlangt jetzt ausdrücklich, vor Abschluss die komplette `<Controls>`-Sammlung des betroffenen Bands durchzugehen — nicht nur die neu erzeugten Elemente — und jedes Kind-Control einzeln zu prüfen. Konkreter Unterfall der bestehenden Vollständigkeits-Heuristik (seit v1.11.0), hier auf „bereits vorher vorhandene Geschwister-Controls" zugespitzt. `known-issues.md` Eintrag 39 ergänzt; neues Eval-Szenario `09-vorbestehende-geschwister-controls.json`.
- v1.11.1 — Nachtrag/Dokumentationskorrektur (Report `dxAio_template`, M001, 10.09.2026): Ein bereits zuvor direkt in `known-issues.md` ergänzter Befund (Eintrag 38: `validate_repx.py` Check `C07` meldet im isolierten Selbst-Audit-Modus ohne `--baseline` Falsch-Positive für Methoden, die ausschließlich in einem Subreport-eigenen `ScriptsSource`-Block definiert sind — Ursache: `C07` sammelt verdrahtete Methodennamen dokumentweit, definierte Methoden aber nur aus dem Hauptskript) war ohne begleitenden Versionsbump/Changelog-Eintrag geblieben. Rein nachgeholte Versionshistorie, keine inhaltliche Änderung an `known-issues.md` selbst in diesem Schritt. Der Check `C07` selbst ist weiterhin NICHT gefixt (offener technischer Punkt, siehe Eintrag 38 „Wie man es künftig vermeidet").
- v1.11.0 — Struktureller Prozessfix nach einer dritten Nachbesserungsrunde am selben Kundenreport (Report `dxAio_template`, M001, 09.09.2026; Nutzerfrage: „Warum musstest du eigentlich so viele Durchläufe machen?"). Drei Maßnahmen, alle vom Nutzer explizit angefordert: **(A) Neuer automatisierter Check `C22`** in `scripts/validate_repx.py` — prüft strukturell (Tag-Stack, keine Regex-Heuristik), dass jedes direkte Kind eines bekannten Sammlungs-Eltern-Tags (`Controls`, `Rows`, `Cells`, `LocalizationItems`, `ExpressionBindings`, `Bands`, `Parameters`, `ParameterPanelLayoutItems`, `StyleSheet`) exakt dem Muster `ItemN` entspricht. Anlass: ein neu angelegter `<LocalizationItems>`-Eintrag mit dem Platzhalter-Tag-Namen `<ItemPLACEHOLDER ...>` ergab gültiges XML, wurde von DevExpress beim Laden aber stillschweigend verworfen und bei einem späteren Rebuild derselben Sammlung endgültig gelöscht — weder `C02`/`C03` (Wohlgeformtheit/Tag-Paarigkeit) noch `C04` (bisheriger ItemN-Lücken-Check, der nur bereits passende Namen zählt) fingen das ab. Empirisch gegen die Referenzdatei geprüft (jedes Tag der Menge hat dort ausschließlich `Item1` als erstes Kind); `Styles` bewusst nicht aufgenommen, da in der Referenzdatei nicht vorhanden und daher nicht bestätigbar. **(B) Neue „Vollständigkeits-Heuristik"** in SKILL.md Schritt 2: Ein zutreffender Katalog-Eintrag mit Plural-/Alle-Formulierung („nachfolgende SubBand(s)", „alle betroffenen Zellen") verlangt jetzt ausdrücklich, VOR Anwendung alle strukturell analogen Kandidaten aufzuzählen und einzeln zu prüfen, statt nur den ersten, symptomnächsten Treffer zu beheben — sichtbar als Liste im Befund an den Nutzer (Schritt 3). Anlass: Muster (h) Punkt 2 wurde in einer Nachbesserungsrunde nur an einer von acht betroffenen Zellen behoben. **(C) Neue Zwei-Rollen-QS-Schleife** als Schritt 6b (Builder/Reviewer, PFLICHT sobald mindestens ein inhaltlicher Fix angewendet wurde): Nach Schritt 6a (automatisierte Validierung) prüft ein unabhängiger, frischer Subagent (via `Agent`-Tool, ohne Zugriff auf die Diagnose-Annahmen oder den Changelog-Entwurf des Builders) die komplette Diagnose eigenständig neu — inkl. erneutem `validate_repx.py`-Lauf und einer gezielten XML-Stichprobe. Weicht das Ergebnis ab, geht es zurück an den Builder (Schritt 3/4), bis der Reviewer sauber bestätigt. Bisheriger Schritt 6 dafür in 6a/6b aufgeteilt (keine Umnummerierung von Schritt 7/8/9, die von anderen Dateien namentlich referenziert werden). `known-issues.md` Einträge 36–37 ergänzt; `validation-checklist.md` Punkt 20 ergänzt (referenziert `C22`).
- v1.10.1 — Wertkorrektur in `fix-catalog.md` Muster (g) Punkt 2 (Report `dxAio_template`, 08.09.2026): Der bisher dokumentierte, am 28.08. bestätigte Laufzeitwert **50** für `tb_ÜbertragOben` (gesetzt im tabelleneigenen `BeforePrint`-Handler `tb_ÜbertragOben_BeforePrint`, analog zu `tb_Überschriften_Folgeseite_BeforePrint`) war laut Testdruck des Kunden zu niedrig; aktuell bestätigt und verbindlich: **70**. Der Seite-1-Zweig (`1f`) bleibt unverändert. `known-issues.md` Eintrag 35 ergänzt — inkl. der Suchmethodik-Lehre, dass eine `<Scripts>`-Verdrahtung einer Tabelle strukturell hinter der ihrer verschachtelten Zellen/Labels liegen kann und ein einfacher „ersten Treffer nehmen"-Scan sie deshalb fälschlich als „existiert nicht" auswerten kann. Referenzdatei (`dxAio_template_REFERENZ_2026-09-07_17-21.repx`) entsprechend korrigiert und neu ausgeliefert; die Kundendatei in M001 wurde in diesem Lauf nicht angefasst (nicht angefordert).
- v1.10.0 — Bugfix in SKILL.md selbst (Report `dxAio_template`, M001, 08.09.2026): Schritt 2 verwies noch wörtlich auf „Muster (a) bis (e)" aus `fix-catalog.md`, obwohl der Katalog seit v1.8.0 bis (j) reicht — ein Rest aus der Zeit vor den Ergänzungen (f)–(j), der beim Katalog-Wachstum nie mitgezogen wurde. Ein Lauf, der dieser Anweisung wörtlich folgte, prüfte eine frische Kundendatei dadurch nur gegen einen Teil des tatsächlich verfügbaren, teils bereits als „automatisch sicher" bestätigten Katalogs (konkret übersehen: Muster (g) Punkt 3, Muster (h) Punkt 2, Muster (j)/`DXJ0001.I`) und stufte die dadurch übrig gebliebenen Differenzen fälschlich als „unrelated evolution" ein, statt sie gegen den vollständigen Katalog zu prüfen. SKILL.md Schritt 2 verweist jetzt ausdrücklich auf „ausnahmslos alle" Muster und auf `fix-catalog.md`s eigenes „Inhalt"-Verzeichnis als verbindliche, immer aktuelle Liste, statt den Umfang selbst zu beziffern. `known-issues.md` Eintrag 34 ergänzt (inkl. der Lehre, dass ein Diff-Fund erst nach Prüfung gegen JEDES Katalog-Muster als „unrelated" ausgeschlossen werden darf); Eintrag 15 um eine Aktualisierung ergänzt (der dort für `dxAio_template` genannte Padding-Top-Wert `10` ist überholt, aktuell bestätigt ist `5`).
- v1.9.0 — Zwei neue, permanente Kontrollpunkte ergänzt (Report `dxAio_template`, M001, 07.09.2026, vom Nutzer explizit als Kontrollpunkte für jeden künftigen Lauf gefordert): **(1) Parameter dürfen nie verändert werden** — konkret beobachtet: in der Referenzdatei `dxAio_template_REFERENZ_2026-09-07_10-44.repx` fehlten alle 8 `LocalizationItems`-Einträge (`Path="Title"`) der Parameter-Panel-Kategorien, obwohl die Kundendatei sie vollständig hatte (vermutlich Nebenwirkung eines Designer-Neu-Saves). **(2) Subreports müssen vollständig eingebettet bleiben** — nach einem früheren Bugfix-Lauf waren sie beim Nutzer nicht mehr eingebettet. Beide Punkte sind jetzt in `validation-checklist.md` als Pflichtpunkte 18–19 sowie in `scripts/validate_repx.py` als automatisierte Checks `C20` (Parameters-Block byte-identisch zur Baseline + keine verlorenen Parameter-Titel) und `C21` (jeder `XRSubreport` trägt ein eigenes, substanzielles `<Bands>`-Gerüst) hinterlegt. `known-issues.md` Einträge 32–33 ergänzt. Die im selben Lauf korrigierte Referenzdatei (`dxAio_template_REFERENZ_2026-09-07_17-21.repx`, 8 fehlende Titel ergänzt) dient als Beleg dafür, dass Punkt 1 real auftritt, nicht nur ein theoretisches Risiko ist.
- v1.8.0 — Neues, vom Kunden bestätigtes Muster (j) in `fix-catalog.md` ergänzt (Report `dxAio_template`, Sektion `Sub_Adresse`, 04.09.2026, Unterpunkt-ID `DXJ0001.I`): ein unsichtbarer Platzhalter blieb trotz korrektem direktem `Visible="false"`-Attribut im Designer sichtbar/raumgreifend, weil ein vorhandener `<LocalizationItems>`-Eintrag (`Path="Visible"`) das Attribut überschrieb — neu bestätigt: dieser Override-Mechanismus gilt auch für `Visible`, nicht nur für `HeightF`/`SizeF`/`LocationFloat`. Fix: Platzhalter inkl. aller Kind-Elemente und eigener `LocalizationItems`-Einträge entfernt, freigewordener Platz über den `<LocalizationItems>`-Eintrag zweier Nachbar-Controls verteilt. `known-issues.md` Einträge 29–31 ergänzt (CRLF-Binär-Bearbeitungspflicht, `Visible`-Override-Fall, Designer-Stale-Cache-Falle — externe Dateiänderung wird in einer bereits geöffneten Designer-Instanz nicht automatisch nachgeladen). `repx-technical-notes.md` (dieser Skill) und `../neuen-devexpress-report-skill-anlegen/references/repx-format-basics.md` (zentrale Basis, da generisch) Abschnitt „Der `<Localization>`-Block" entsprechend erweitert. Neues Eval-Szenario `06-visible-localization-override.json`.
- v1.7.0 — Qualitätsmaßnahme „eine Quelle je Information": Die Prosa-Zusammenfassung der neun Muster in `SKILL.md` entfernt, weil sie dieselbe Information ein drittes Mal führte (neben der ID-Tabelle in derselben Datei und `fix-catalog.md`) und bereits zweimal auseinandergelaufen war — sie beschrieb die Skript-Hygiene noch als optional, obwohl Schritt 5 sie als Pflicht führt, und kannte die Auflösung des Konflikts zwischen Muster (g) und (i) nicht. Die ID-Tabelle trägt jetzt zusätzlich die Sicherheitsstufe und ist die verbindliche Kurzliste; Details ausschließlich im Katalog. Neue Linter-Checks `S11` (Unterpunkt-IDs stimmen über `SKILL.md`, `fix-catalog.md` und `uebersicht.md` überein) und `S12` (Versionsangabe stimmt über Frontmatter, Registry und Inhaltsverzeichnis überein) sichern beides maschinell ab; `S06` ist von einer Größen-Warnung auf einen reinen Informationswert umgestellt — Qualität geht vor Token-Zahl.

- v1.6.0 — Abgleich mit der offiziellen Agent-Skills-Spezifikation: `skill_id`/`version` unter `metadata:` verschoben (als Top-Level-Keys beim claude.ai-Upload nicht erlaubt), skillübergreifende Verweise auf `fix-log-format.md`/`unterpunkt-ids.md`/`skill-id-registry.md` mit vollständigem relativem Pfad korrigiert (zeigten vorher ins Leere), Skript-Aufruf über `${CLAUDE_SKILL_DIR}`, Inhaltsverzeichnisse in `fix-catalog.md` und `known-issues.md`, diese Versionshistorie aus `SKILL.md` hierher ausgelagert (~1.800 Tokens weniger pro Trigger), fünf Evaluations-Szenarien in `evals/` aus den realen Fehlerfällen der Sitzung 03./04.09.2026.

- v1.0.0 — Erstfassung plus nachträgliche Erweiterungen vor Einführung dieser Versionshistorie (Registry-Eintrag `DXJ0001`, 2026-08-28): Diagnose-/Fix-Methodik mit drei Sicherheitsstufen (Muster a-f), optionale Skript-Hygiene, Validierungs-Checkliste, `known-issues.md` als lebendes Dokument; danach ergänzt um Muster (g) „Mindesthöhen statt KeepTogether", die verpflichtende Referenz-.repx-Anforderung vor jedem inhaltlichen Fix, und die Sektion „Bekannte Grenzen".
- v1.1.0 — Neuer Schritt 7 „Log-Eintrag schreiben" ergänzt: schreibt jetzt bei jedem abgeschlossenen Lauf einen Eintrag im `work4all-skill-log`-Block, auch ohne Änderung (`keine Änderung nötig`), gemäß `fix-log-format.md` v2. Schritt 8 „Auslieferung" um einen verpflichtenden Statusbericht zu nicht ausgeführten Teilen ergänzt. Schritt 1 und 2 um explizite Abbruch- bzw. Notwendigkeits-Klarstellung ergänzt. Abschnitt „Skill-ID, Version & Fix-Log" auf das neue `(v2)`-Log-Format samt `<Ergebnis>`-Feld aktualisiert und auf Schritt 7 verweisend gekürzt (statt Detail-Duplikat).
- v1.1.1 — Dokumentationskorrektur: Log-Block in `work4all-log` umbenannt (vorher `work4all-skill-log`), Versionskennzeichnung `(v2)` in der Kopfzeile bleibt erhalten. Zeitstempel-Vorgabe in `fix-log-format.md` korrigiert: kein UTC-Offset (`+02:00`) mehr im Zeitstempel-Feld — nur noch die lokale Wanduhrzeit Europe/Berlin ohne Offset-Suffix. Alle Verweise auf den Blocknamen in dieser Datei entsprechend angepasst.
- v1.1.2 — Fehlerkorrektur in `repx-technical-notes.md` Schritt 4.3 (Bearbeitungs-Pipeline): Zeilenenden werden jetzt explizit auf reines `\n` normalisiert, BEVOR die finale Kodierung `\n` → `&#xD;&#xA;` erfolgt. Ohne diesen Schritt blieb bei jedem Zeilenumbruch im gesamten Skript ein rohes `\r` vor dem neu kodierten `&#xD;&#xA;` stehen, sichtbar als Leerzeile nach jeder Zeile im Designer/Editor (siehe `known-issues.md` Eintrag 13). Neuer Validierungspunkt 12 in `validation-checklist.md` ergänzt (keine rohen `\r`/`\n` im re-escapten `ScriptsSource`-Wert).
- v1.1.3 — Neues, vom Kunden per Testdruck bestätigtes Muster (h) in `fix-catalog.md` ergänzt: `AllowMarkupText="true"` auf einer wachsenden mehrzeiligen Zelle ohne tatsächlichen Markup-Bedarf kann eine textlängen-abhängige (nicht seitenumbruch-abhängige) zusätzliche Leerzeile erzeugen; Folge-Fix erhöht ggf. das Top-Padding der unmittelbar nachfolgenden SubBand-Zeile. Details und Diagnose-Historie in `known-issues.md` Einträge 14 und 15.
- v1.2.0 — Neues, vom Kunden über eine lange iterative Diagnose-Reihe bestätigtes Muster (i) in `fix-catalog.md` ergänzt: Seite-1-Leerraum bei inhaltlich korrekt unterdrückten Subbändern (vier zusammenwirkende Teilursachen: fehlendes bandeigenes `BeforePrint`, dynamisches statt statisches `CanGrow`, unabhängig persistierte Höhen verschachtelter Controls, wirkungsloses direktes `HeightF`-Attribut auf Bandebene statt `<Localization>`-Eintrag). Details und widerlegte Zwischenhypothesen in `known-issues.md` Einträge 16–20. Neuer Abschnitt „Unterpunkt-IDs" (Meta-Skill-Baustein 11, `DXJ0001.A`–`H`): jedes Muster bekommt eine eigene ID, Schritt 3 zeigt zutreffende IDs zur gezielten Abwahl, Schritt 7 protokolliert eine Abwahl im `work4all-log` (jetzt `(v3)`-Format). Muster (e) (Skript-Hygiene, Schritt 5) um vier konkrete, bestätigte Bereinigungsfälle erweitert: work4all-interne Sonderparameter, ein wirkungsloses reines Seite-1-Abstands-Band, reine Debug-String-Ausgaben, Kommentare kürzen statt löschen. Nebenbei Inkonsistenz in Schritt 5 korrigiert: die Überschrift „Optionale Skript-Hygiene" stand im Widerspruch zu `fix-catalog.md`, wo Muster (e) bereits seit 28.08. als PFLICHT-Bestandteil jedes Laufs eingestuft ist — Schritt 5 jetzt entsprechend als Pflicht (mit gezielter Abwahlmöglichkeit über die neue Unterpunkt-ID-Vorschau) formuliert.
- v1.5.0 — Selbst-Check nach der Sitzung: drei Mängel in der eigenen Dokumentation behoben und eine Regel ergänzt. (a) Widerspruch zwischen Muster (g) („Höhe nie verringern") und Muster (i) („Design-Höhe auf Mindestmaß absenken") aufgelöst — bei gemeinsamer Anwendung wandert der Wert vom Design in die Laufzeit; (b) Schritt 2 um eine **Pflicht-Vorprüfung der Referenzdatei** erweitert (Freigabefassung vs. Diagnose-Zwischenfassung, Validator-Lauf auf der Referenz selbst); (c) **Zwei-Wege-Kommentarregel** in Hygiene-Regel 10: Referenzdatei behält vollständige Kommentare, nur die Live-Datei wird gekürzt, und zwar als eigener, ausdrücklich angeforderter Schritt — danach Referenz-Diff kommentar-unempfindlich führen. Neuer Check `C19` (Log-Block append-only) sichert die Kürzung ab.
- v1.4.1 — Nachtrag aus derselben Sitzung: `Padding` wird als `Left,Right,Top,Bottom,Dpi` serialisiert (Top ist Position 3). Ein im Referenz-Changelog als „Top"-Fix dokumentierter Padding-Wert saß auf der Right-Position und war vertikal wirkungslos — übernommen und damit ebenfalls wirkungslos angewendet. Neuer Check `C17` leitet die Reihenfolge bei jedem Lauf aus den `Padding.LeftF`/`Padding.RightF`-Bindungen der Datei selbst ab; `known-issues.md` Eintrag 28, Muster (h) und Checkliste Punkt 17 entsprechend ergänzt.
- v1.4.0 — Sitzung 03./04.09.2026 am Report `dxAio_template`: vier neue, jeweils erst durch den Kunden im Designer/Testdruck gefundene Fehlerklassen dokumentiert und automatisiert absicherbar gemacht. Neu: **`scripts/validate_repx.py`** als Check-Index (`C01`–`C19`), verbindlich nach jeder Bearbeitungsrunde und zusätzlich als Selbst-Audit auf der Referenzdatei (Schritt 6). `known-issues.md` Einträge 22–27 ergänzt: Lücken in der `ItemN`-Nummerierung machen nachfolgende Einträge wirkungslos (22); ein in `PrintOnPage` gesetztes Flag ist in **jedem** `BeforePrint` immer `false`, auch auf Control-Ebene (23); abgesenkte Design-Höhen und Laufzeit-Wiederherstellung gehören zwingend zusammen, `HeightF` in `PrintOnPage` ist wirkungslos (24); ein Muster mit sich kompensierenden Teilfixen wird nur vollständig angewendet, und Zähl-Prüfungen sind kein Nachweis für ein bestimmtes Element (25); ein auffälliger Design-Wert kann durch ein `ExpressionBinding` überschrieben sein und ist dann kein Bug (26); eine Referenzdatei kann selbst fehlerhaft sein — Diagnose-Zwischenfassungen nie 1:1 als Code-Vorlage nehmen (27). `fix-catalog.md` Muster (g)/(h)/(i) entsprechend korrigiert, `validation-checklist.md` um die Pflichtpunkte 13–16 und den Skript-Aufruf erweitert.
- v1.3.0 — Bestätigter Befund (Report `dxArticleList`, DXJ0002-Skill, 03.09.2026): ein rein aus Kommentaren bestehender `work4all-log`-Block wird beim Speichern aus dem DevExpress Report Designer restlos entfernt. Schritt 7 prüft jetzt bei jedem Lauf zusätzlich, ob die neue Anker-Zeile (`_work4allLogAnchor`, siehe `fix-log-format.md` Regel 8) vorhanden ist, und rüstet sie bei älteren Dateien nach. Schritt-5-Hinweis und Skill-ID-Abschnitt entsprechend ergänzt, damit die Skript-Hygiene die Anker-Zeile nicht versehentlich als toten Code entfernt. Details: `known-issues.md` Eintrag 21.
